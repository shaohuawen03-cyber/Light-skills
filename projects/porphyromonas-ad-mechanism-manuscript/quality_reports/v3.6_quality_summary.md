# v3.6.0 manuscript quality summary

## Deliverables

- Full English and Chinese DOCX: 4 tables each, 55 references each.
- Concise English and Chinese DOCX: 2 tables each, 22 references each.
- All four files are titleless, abstract-first, figure-free, and use neutral core metadata.
- Conclusion is followed directly by References in every variant.

## Structural results

`submission_manuscript_audit.json`: **PASS**

The audit confirms:

- no Declarations or intervening administrative sections;
- no Tier A/B/C labels, hashes, commit identifiers, archive/acceptance records,
  or workflow-governance prose in article bodies;
- no specific participant, specimen, assembly-analysis, or MAG totals;
- no Markdown figure markup, OpenXML drawing objects, image relationships, or
  `word/media/` members;
- every table has only a top rule, a rule beneath the header, and a bottom rule;
- no vertical borders, interior body rules, or table shading;
- full and concise reference lists follow citation order and are synchronized
  between English and Chinese;
- all source citation keys resolve to `references/references.bib`;
- prospective 100-ns MD methods are present and MD results are absent.

`full_docx_reproducibility.json`: **PASS** — all four released DOCX files are
byte-identical to isolated deterministic rebuilds.

## Citation state

The Markdown sources use Pandoc/BibTeX citation keys and the standard DOCX build
automatically resolves them to numbered Vancouver-style citation text. The
current DOCX packages contain zero `ADDIN ZOTERO_ITEM CSL_CITATION` fields and
are therefore not represented as Zotero-live.

`zotero_live_status.json`: **setup gate**

- Better BibTeX JSON-RPC endpoint: not exposed in the build environment.
- Pandoc: not exposed in the build environment.
- Desktop Word/Zotero refresh: pending user action.

The strict live-field builder and user gate are documented in
`references/ZOTERO_WORD_ACCEPTANCE.md`. A refreshed Word file must be returned
for independent live-field audit before the state can become “returned file
independently audited.”
